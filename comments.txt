/* Создание схем raw_date и car_shop. 
 * Добавление в схему raw_date таблицу sales.
 * Добавление в схему car_shop таблиц: brands, models, cars, colors
 * cars_colors, clients, sales, phone_numbers, phone_extensions, discounts */


/*
 * Заполнение таблиц
 * 
 * 
 * Сначало решил нормализовать атрибут auto таблицы sales. Так как он содержит строку хранящую сразу три
 * важные информации во 1 это брэнд, 2 это модель, 3 это цвет.
 * 
 * Поэтому я решил сделать следующию структуру:
 * 
 * таблица cars будет содержать следующую структуру у неё будет 3 атрбибута model_id, price, 
 * gasoline_consumption, model_id будет ссылаться на таблицу models та в свою очередь будет ссылаться на
 * таблицу brands тем самым получиться уменьшить дублируемость данных, так как количество брендов машин,
 * относительно мало. 
 * 
 * Так же я решил цвет машины венести в отдельную строку так как связь между colors и
 * cars многие ко многим я ввел таблицу связи cars_colors она будет как раз обеспечивать, что у каждой
 * машины может быть много цветов раскраски так и у цвета может быть много машин с этим цветом
 * 
 */


/*
 * Так же с решил сделать нормализацию с клиентами следующим образом:
 * 
 * Таблица clients будет содержать следующие атрибуты first_name, last_name, на эту таблицу будет ссылаться
 * дочерняя таблица phone_numbers, которая содержит номер без добавычных, а так же у таблицы phone_numbers
 * есть своя дочерняя таблица phone_extensions, которая хранит добавочные номера
 * 
 * Так же у таблицы clients будет еще одна таблица дочерняя discounts обеспечивает связь один ко одному
 * -- таблица sales будет таблицей фактов, которая будет хранить данные о сделках 
 */




Вопрос здравствуйте хотел спросить насколько уместно выносить атрибут gasoline_consumption в отдельную таблицу просто я
посмотрел у меня у запросов столько join обычно много, что кажется как будто слишком, тем более электро машин не так много
Так же хотел спросить глупый вопрос: у меня есть таблица sales можно ли её считать таблицей фактов или в ней очень мало атрибутов, 
просто я читал про информацию проектирования баз данных и узнал, что есть два основных типа проетирования звезда и снежинка,
и получается по построению снежинка, и вот вопрос будет это вообще звезда или снежинка или я что то путаю?
